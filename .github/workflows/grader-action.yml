name: Grader Assessment Action

on:
  workflow_dispatch:
  push:

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: 24f1002239@ds.study.iitm.ac.in
        run: echo "Hello, Grader!"
